using Godot;
using System;
using System.Collections.Generic;

public class Level01 : StaticBody2D
{
    //public Dictionary<int,Shield> ShieldPositions;

    //// Called when the node enters the scene tree for the first time.
    //public override void _Ready()
    //{
    //    ShieldPositions = new Dictionary<int, Shield>();
    //    ShieldPositions.Add(1, new Shield {RotationAngle = 0, PositionPoint = GetNode<Position2D>("ShieldPosition1") });
    //    ShieldPositions.Add(2, new Shield { RotationAngle = 0, PositionPoint = GetNode<Position2D>("ShieldPosition2") });
    //    ShieldPositions.Add(3, new Shield { RotationAngle = 0, PositionPoint = GetNode<Position2D>("ShieldPosition3") });
    //    ShieldPositions.Add(4, new Shield { RotationAngle = 0, PositionPoint = GetNode<Position2D>("ShieldPosition4") });
    //}

    //  // Called every frame. 'delta' is the elapsed time since the previous frame.
    //  public override void _Process(float delta)
    //  {
    //      
    //  }
}
